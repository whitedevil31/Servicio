// import { NextFunction, Request, Response, Router } from "express";

// interface errorFormat {
//   error: string;
//   status: number;
// }

// export const errorHandler = (
//   err: errorFormat,
//   req: Request,
//   res: Response,
//   next: NextFunction
// ) => {
//   res.status(err.status).json({ success: false, message: err.error });
// };
